function welcomeUser(username){
	if(username == undefined){
		return "welcomeback" + "!";
		return "Welcome Back" + username + "!";
	}
}

function sum(x, y){
	if (x== undefined)
		return y;
	if (y == undefined)
		return x;
	return x+y;
}